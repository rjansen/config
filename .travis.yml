language: go

go:
    - 1.11.x
      # cache:
      #     - vendor
      #     - on
      #     - tmp
      # 
      # install: sudo make deps
      # 
      # script: make all

before_install: make docker

script: make docker.all

notifications:
  email:
    on_success: change
    on_failure: always

services:
  - docker
